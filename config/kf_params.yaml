kf_vio_pnp_node:
  ros__parameters:
    accel_noise_std: 1.0
    bias_rw_std: 0.02 # random walk std for position bias, unit (m/s per sqrt(s))
    vio_pos_std: 0.20
    vio_vel_std: 0.30
    pnp_pos_std: 0.03

    # debug
    mocap_as_pnp: true